[package]
name = "sse_benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = { version = "0.17.2" }
num-complex = "0.4.5"
sse_solver = { version = "0.1.0", path = "../sse_solver", features = ["serde"] }
rand = "0.10.1"
rand_distr = "0.6.0"
