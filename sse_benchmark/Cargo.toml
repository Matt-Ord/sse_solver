[package]
name = "sse_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
matrixmultiply = { version = "0.3.8", features = ["cgemm"] }
ndarray = { version = "0.15.0" }
num-complex = "0.4.5"
sse_solver = { version = "0.1.0", path = "../sse_solver" }
